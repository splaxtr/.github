{
  "name": "bun-app",
  "version": "1.0.0",
  "scripts": {
    "lint": "echo linting",
    "format:check": "echo formatting",
    "type-check": "tsc --noEmit",
    "test": "bun test",
    "build": "bun build src/index.ts --outdir dist",
    "test:e2e": "echo e2e"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
